# Machine-specific configuration.
#
# Copy to 90-local.fish and add anything that should not be in the repo:
# API tokens, host-specific paths, work-only settings. The 90 prefix means it
# is sourced after everything else, so it can override any earlier module.
#
# Add to .gitignore:
#     .config/fish/conf.d/90-local.fish

# set -gx SOME_API_TOKEN "..."
# fish_add_path -g /opt/something/bin
