function mcr --wraps='docker compose down && docker compose up -d && docker compose logs -f --tail=0' --description 'alias mcr=docker compose down && docker compose up -d && docker compose logs -f --tail=0'
    docker compose down && docker compose up -d && docker compose logs -f --tail=0 $argv
end
